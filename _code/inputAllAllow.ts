static config : ControllerConfig = {

    input : {},

    //False is the default value.
    //With false, the input rules are used.
    //In this example, that means that no input is allowed.
    inputAllAllow : false,

    //With true, the input rules are not used,
    //and every input is allowed.
    inputAllAllow : true,
};