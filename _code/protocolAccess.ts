static config : ControllerConfig = {

    //The requests with the protocol HTTP are allowed.
    httpAccess : true,

    //The requests with the protocol HTTP are not allowed.
    httpAccess : false,

    //The requests with the protocol WebSocket are allowed.
    wsAccess : true,

    //The requests with the protocol WebSocket are not allowed.
    wsAccess : false

};