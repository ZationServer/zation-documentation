start
(
    {
        //Here we rename the configs folder to myConfigs
        configs : 'myConfigs',

        //Here we rename the service configuration
        serviceConfig : 'myService.config'
    }
);